<div class="step-2">
  <h2>Step 2
  <br>Pros & Cons</h2>
  <br>
  <p>What are the positive and negative aspects about <%= @option.option_name %>? <br>How does each aspect make you feel?</p>
</div>
<%# <%= link_to 'Make Argument', new_option_argument_path(@option)%>

<div class="container">
  <div class="content">
    <div class="input">
      <div>
        <% @option.arguments.each do |argument| %>
          <ul>
            <li><%= argument.argument_name %> <%= argument.score %></li>
          </ul>
        <% end %>
    </div>

    <div>
      <%= simple_form_for [@option, @argument] do |f| %>
        <%= f.input :argument_name, label: 'description' %>
        <%= f.input :score, label: 'score (very negative: -2, very positive +2)' %>
        <%= f.submit "Add argument", class: "btn btn-primary" %>
      <% end %>
    </div>

    <div class="mood-buttons">
      <button class="mood-button">😢</button>
      <button class="mood-button">🙁</button>
      <button class="mood-button">🙂</button>
      <button class="mood-button">😀</button>
    </div>

    <div class="buttons">
      <%=@next_option%>
      <p class="show-buttons"><%= link_to 'Back', :back %></small></p>
      <p class="show-buttons"><%= link_to 'Delete Option', option_path(@option), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></small></p>
      <p class="show-buttons"><%= link_to 'Next', option_path(@option) %></small></p>
    </div>
  </div>
</div>
