<div class="step-2">
  <h2>Step 2 
  <br>Pros & Cons</h2>
  <br>
  <p>What are the positives and negatives aspects about <%= @option.option_name %>? <br>How does aspects makes you feel?</p>
</div>
<%# <%= link_to 'Make Argument', new_option_argument_path(@option)%>

<div class="container">
  <div class="content">
    <div class="input">
      <div>
        <% @option.arguments.each do |argument| %>
          <ul>
            <li><%= argument.argument_name %> <%= argument.score %></li>
          </ul>
        <% end %>
    </div>

    <div>
      <%= simple_form_for [@option, @argument] do |f| %>
        <%= f.input :argument_name, label: 'description' %>
        <%= f.submit "Add", class: "btn btn-primary" %>
      <% end %>
    </div>

    <div class="mood-buttons">
      <button class="mood-button">😢</button>
      <button class="mood-button">🙁</button>
      <button class="mood-button">🙂</button>
      <button class="mood-button">😀</button>
    </div>
    
    <div class="buttons">
      <%=@next_option%>
      <p class="show-buttons"><%= link_to 'Back', :back %></p>
      <p class="show-buttons"><%= link_to 'Next', option_path(@option)%></p>
    </div>
    </div>
  </div>
</div>